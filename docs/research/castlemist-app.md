---
name: castlemist-app
description: "castlemist Win32/D3D11 dat viewer — features, module map, and the direct-g++ build recipe"
metadata: 
  node_type: memory
  type: project
  originSessionId: fc3b5516-fefe-4ef8-bad1-3bb3c57e6cfd
  modified: 2026-07-25T23:24:37.814Z
---

`castlemist/` is a Win32 + D3D11 Gw2.dat browser (3 columns: MFT list │ tabbed Compressed/Decompressed hex + adaptive **Preview** │ info panel).

**AUDIO PLAYBACK (2026-07, ASND → MP3 via dr_libs).** GW2 sound effects/voice = PF packfiles, container **`ASND`** (PF v1), chunk ASND → `AsndFileDataV0/1/2` → `audioData` array_ptr = a complete **MP3** file (0xFF frame sync + Xing/Info VBR header; `format` byte=1=MP3). Verified: ASND idx 2861 = MP3 offset 92, 3712 B, 48000 Hz 1ch 0.38 s (matches the chunk's `length`/`numSamples`). **`AMSP`** (PF v5) = sound POOL/script metadata (soundPoolCategory/soundScript), **no embedded samples** → not playable. No `ABNK` container in this dat. **Parser** `gw2mcp/native/gw2_audio.hpp` (header-only, no deps): `isAudio`/`extract` — verifies PF+container, then locates the audio by **file signature** within the payload (ID3 / valid MPEG frame / OggS / RIFF-WAVE / fLaC), returns the blob to EOF (version-independent; trailing pad bytes ignored by decoders). AMSP → 0 clips. **Player** `castlemist/src/audio_player.cpp` (the ONE `DR_MP3_IMPLEMENTATION` TU, uses `dr_libs-master/dr_libs-master/dr_mp3.h`): `castlemist::snd::probe/play/stop/is_playing/shutdown` — decode MP3→s16 PCM (`drmp3_init_memory` auto-finds the first frame), play via **Win32 waveOut** (single async buffer + WOM_DONE callback flag; `-lwinmm`). **UI**: new `PreviewKind::Audio` + `ExtractedEntry.audio_data/audio_codec`; detection in `decompress_raw_entry` after the strs check; audio info (codec/Hz/ch/duration/bytes) shown in the text-preview surface + **▶ Play / ■ Stop** buttons (ID_AUDIO_PLAY/STOP 2056/57, shown when `show_audio`). `apply_extracted_entry` calls `castlemist::snd::stop()` on every entry switch; `castlemist::snd::shutdown()` on WM_DESTROY. Verified end-to-end (env `GW2_AUDIOTEST=<idx>` → detect+probe+play, logs audiotest.txt): idx 2861/2865/2869/3166/3178 all play=1 (0.35-1.25 s). **Build now also needs `-I ../dr_libs-master/dr_libs-master` and `-lwinmm`.**

**AUDIO BANKS + RAW asnd (2026-07).** `gw2_audio.hpp` now returns MULTIPLE clips via **MP3 frame-walking** (`mp3FrameLen` with full MPEG1/2/2.5 bitrate/samplerate tables): each maximal run of ≥3 contiguous frames = one sound. **ABNK** (container ABNK, chunk BKCK, PF v5) = a bank of N MP3s each preceded by a ~128 B header/gap → split cleanly (verified fileId 159071 = **10 sounds**, 2.5-6 s each). **Raw `asnd`** (magic `asnd`, NOT a PF; e.g. fileId 756508) = 36 B header + MP3 → signature scan finds it at off 36. `ExtractedEntry.audio_clips` = `std::vector<AudioClipCPU>{codec,data}`; UI adds a **sound-selector combo** (ID_AUDIO_COMBO 2058, shown when >1 clip; `audio_selected_index()`/`update_audio_info()`); selecting a sound stops playback + shows its info. **AMSP** = a sound BANK/script (PF v5, **64-bit ptrs**, chunk AMSP v33) whose audio lives in EXTERNAL ASND entries referenced by fileId — now SUPPORTED. `entry_extractor.cpp` `parse_amsp_sound_ids`: metaSound array_ptr @chunk+60 {u32 count, i64 selfRelPtr}; `MetaSoundDataV31` stride 326, fileName array_ptr @elem+60; `FileNameDataV31` stride 36, fileName field @fe+24 = a **64-bit self-relative ptr** to {u16 lo,u16 hi} → fileId=`0xFF00*hi+lo-0xFF00FF`. `build_amsp_audio` builds a fileId→baseId map once (O(1)), loads each referenced ASND (cap 512 unique), `castlemist::audio::extract`s the sound, appends to `audio_clips` → playable multi-sound bank. Verified: AMSP 9235/18312/831542/87633/69550 → 451-512 clips each. **Ogg Vorbis support added** (some AMSP sounds are Ogg, e.g. 87633 music): `audio_player.cpp` `decode_clip` dispatches MP3(dr_mp3)/Ogg(**stb_vorbis**, extern-C, `src/stb_vorbis_impl.c`)→s16 PCM. Env `GW2_AUDIOTEST=<mftIdx>`.

**PIMG PAGED-IMAGE ATLAS → composited map/atlas preview (2026-07).** PIMG (PF, chunk **PGTB** v3) is NOT pixels — it's a grid of tiles, each an EXTERNAL texture referenced by fileId at a (cx,cy) coord. `entry_extractor.cpp` `parse_pimg` (V3): PGTB fields at `pos+headerSize`; `layers[0].strippedDims`=tile WxH, `strippedFormat`=fourcc (e.g. DXT5); `strippedPages` array (24 B/page: layer, coord(cx,cy)@+4, **filename@+12**, flags, solidColor). **KEY: the `filename` field is a self-relative ptr to a {u16 lo,u16 hi} fileref; fileId = `0xFF00*hi + lo - 0xFF00FF`** (mirrors `castlemist::model::decodeFilenameAt` — a raw dword read gives WRONG ids). `composite_pimg`: downscale the whole atlas to fit kMaxDim=2048, decode up to kMaxTiles=300 pages (subsample beyond) via `decode_texture_by_fileid`, nearest-blit each tile into the RGBA canvas at coord·tile·scale → set as `PreviewKind::Image`. Verified: small atlas 3010939 (80 tiles) = full zone map; giant map 100169 (67×104=1570 tiles) previews in ~12 s. Env `GW2_PIMGTEST=<mftIdx>` dumps pimg_preview.bmp. Tiles are often **ATEP** (ATEX variant, handled by gw2atex).

**cntc = GW2 content datastore → summary text preview (2026-07).** Container `cntc` (PF v5, 64-bit ptrs, single chunk **`Main`** = `PackContent`): a content database, not a viewable asset. `entry_extractor.cpp` `parse_cntc_summary` reads the PackContent header — flags@base, then 11 `array_ptr`s {u32 count, i64 self-rel ptr from field+4} @ base+4+i·12 in order: typeInfos, namespaces, fileRefs, indexEntries, localOffsets, externalOffsets, fileIndices, stringIndices, trackedReferences, **strings**(=wchar_ptr[], content codenames like `FOyVJ.AL3Ye`), **content**(byte[], the ~8.5MB blob). Shows section counts + namespace names (if any; PackContentNamespace = wchar_ptr+domain+parentIndex, 16B) + first 60 codenames → `PreviewKind::Text`. Verified cntc 1282844/1282837/1282833 (21-22k codenames, 8.8MB content each). Local content files have 0 types/namespaces (those live in a master manifest). **cntc IS an asset DB that references textures/models/audio by fileId — PROVEN + shown:** each `fileIndices` fixup {u32 relocOffset} marks a spot in the `content` byte-array holding a **raw fileId** → `fileId = rd32(contentDataOff + relocOffset)`. `cntc_referenced_asset_ids` collects them (>40000, dedup); `classify_fileid` peeks each (decompress header → PF container/GEOM=model, ATEX*/DDS=texture, ASND/ABNK/AMSP=audio, AMAT, strs); the cntc preview appends "References N external asset fileIds" + a sampled type breakdown. Verified 1282844 → 2178 refs, sample = 73 texture / 22 model / 17 audio / 1 AMAT. Full content decode (codename→game object schema, per PackContentTypeInfo) still out of scope. See `RESEARCH_FILETYPES.md` §2c.

**INTERACTIVE cntc CONTENT BROWSER (2026-07).** cntc is now `PreviewKind::Content` (not Text): `ExtractedEntry.content_asset_ids` = ALL referenced fileIds. UI (main.cpp) splits the preview area: a **listbox `hwnd_content_list`** (ID_CONTENT_LIST 2059, ~300px, left) of "fileId N" items + the existing image/model/text surfaces in the right pane (offset by `clist_w`; layout uses `pane_x`/`pane_w`). Clicking an item → `load_content_asset` → `extract_entry(dat, base-1)` into `content_sub` → `render_content_sub` routes it: texture→castlemist::gfx::set_texture, model→castlemist::render::set_model(Full), audio→combo+info (Play uses `active_audio_entry()` = content_sub in content mode), else→text. Visibility driven by `ek` (content_sub.kind) / `se` (active entry). No open/close — inline side preview. Verified 1282844: click loads real textures (128²/256²/512²), models, audio sub-entries. `content_sub_loaded` gates it; `castlemist::snd::stop()` on each pick. Env `GW2_PIMGTEST=<cntcIdx>` logs sub-entry kinds.

**cntc browser reworked to 3-panel Types → Entries → Preview (2026-07-16, per user request "daftar per tipe → klik → daftar → klik → preview").** The two cntc listboxes were repurposed in main.cpp: **master `hwnd_content_list`** now lists the DISTINCT content TYPES (one row per type + object count, e.g. "Item  (1291)"); **child `hwnd_content_child`** lists the ENTRIES (objects) of the clicked type ("id N   (K assets)"); clicking an entry previews its **primary (first) asset** in the right pane (multi-asset entries also dump their full fileId+kind list into the text overlay). `content_objects` is pre-sorted by (type,id) so `populate_content_master` groups contiguous types into `AppState.content_types` (vector<pair<type,count>>); `on_content_master_select` filters objs by the selected type into `content_child_objidx` (child-row → content_objects index); `on_content_child_select` maps back and calls `load_content_asset_fid(assets[0])`. Master/child widths 185/210. **Multi-asset ENTRY picker (2026-07-16 follow-up, user: "cant choose which asset"):** entries can reference up to 16 assets; a new **`hwnd_content_asset_combo`** (ID_CONTENT_ASSET_COMBO 2082, right-aligned in the preview toolbar band, shown when the selected entry has >1 asset) lists each asset ("Asset i/N — fileId kind"); `on_content_asset_select` (CBN_SELCHANGE) → `load_content_asset_fid(assets[sel])` re-renders the chosen one. Layout `content_multi` gates visibility (from `content_obj_sel`'s asset count) and drops the text control below the toolbar so the combo stays visible. Verified headless: type 18 id 1 (16 assets) → cycling the combo loaded each distinct fileId as its own surface (textures 512²/256², kind=2 models at combo 10/13, other at 12/14). `content_type_name` (entry_extractor.cpp) enriched with IDA-verified + T3D-observed labels: 0 Achievement, 1 Achievement Category, 12 Crafting Recipe, 27 Guild Upgrade, 35 Item, 45 Map, **48 Minipet** (IDA CONTENT_TYPE_MINIPET==48, WardrobeSlot.cpp:547), 51 Outfit, 64 Skill, 66 Skin; unknown → "Type N". Verified headless (temporary GW2_CNTCTEST hook, since removed): cntc mft **805147** (fileId 3788541) → 8227 objects / 34 types; type list + per-type entries + per-entry texture preview (512²/128²) all correct, unnamed/non-renderable types fall through cleanly. See [[gw2-chat-links]].

**cntc browser → 3 SORTABLE ListView TABLES (2026-07-16, user: dropdown covered the model toolbar buttons).** The two content LISTBOXes + the floating `hwnd_content_asset_combo` (which right-aligned over the preview toolbar and hid Skeleton/Play/etc at small widths) were replaced by three `WC_LISTVIEWW` report-view tables in main.cpp: **`hwnd_content_list`** Types(name|count) and **`hwnd_content_child`** Entries(id|assets) side by side on top; **`hwnd_content_asset_list`** (ID_CONTENT_ASSET_LIST 2082, was ID_CONTENT_ASSET_COMBO) Assets(#|fileId|kind) stacked full-width **below** them (layout: `avail=content_h-gap`, `assets_h=clamp(avail*35/100, min(avail,96), min(avail,300))`, `top_h=avail-assets_h` — exact fill, never spills; master/child widths 175/235). Asset table is a real left-column pane now, so NOTHING floats over the toolbar at any window size (the reported bug). Each row's **lParam = a stable backing index** (Types→content_types idx, Entries→content_objects idx, Assets→asset idx) so selection + sorting stay in sync. Events moved WM_COMMAND(LBN/CBN)→**WM_NOTIFY**: LVN_ITEMCHANGED (new-select only: `uChanged&LVIF_STATE && uNewState&LVIS_SELECTED && !uOldState`) drives drill-down; **LVN_COLUMNCLICK** → `content_sort_click`→`ListView_SortItems(content_lv_compare)` (toggles asc/desc; `content_sort_col[3]`/`content_sort_asc[3]` in AppState). Entry-pick fills the asset table then `ListView_SetItemState(row0,SELECTED)` → its LVN_ITEMCHANGED is the single load path. Helpers `lv_add_col/lv_add_row/lv_selected_param`. Builds clean; startup smoke-tested. Full cntc drill-down still verified interactively by user (87GB dat).

**prlt / mfst = ContentPortalManifest (2026-07):** container `prlt` (PF v1) wraps chunk **`mfst`** v2 = `ContentPortalManifestV*` — a large (0.7-1.2 MB) content-dependency/manifest data table (portals→assets), NOT visual/audio. No preview; hex view is the correct fallback. (User's "mfst" label = the chunk; the container is prlt.)

**INDEX-DB IMPORT (parse vs index) + type/container FILTER (2026-07, Stage 2).** Two data sources: **File→Open .dat** (parse live, as before) and **File→Open Index DB…** (ID_FILE_OPEN_INDEX 1007) = a gw2index/gw2local SQLite (see [[gw2index-tool]]). New `index_db.{h,cpp}` (`gw2idx`, links `../gw2index/sqlite3.o`, `-I ../gw2index`): open/close, dat_path(meta), entry_count, types()/containers() (distinct), load_meta_map(cb per base_id), query_base_ids(type,container,id,byFileId,idActive,limit) via dynamic SQL, lookup(base_id)→{type,container,sizes,comp,chunks}. `do_open_index` opens DB, **auto-loads the .dat at meta.dat_path** (refactored `load_dat_path(hwnd,path)` out of do_open_file) for previews, then builds an interned `base_id→(typeIdx<<16|contIdx)` map (`g_app->index_meta`, ~3MB for 807k) for two NEW list columns (mft_listview kColumnCount 7→9 "Type"/"Container" via `set_metadata_provider`), and fills Type/Container filter combos (ID_FILTER_TYPE 2024/CONTAINER 2025, hidden until index loads, row at y=60, kSearchBarHeight 64→92). `apply_filters()` (replaces do_search body): index mode → `castlemist::db::query_base_ids` → `set_filter`; parse mode → id-only dat search; combos fire CBN_SELCHANGE→apply_filters. Verified: query cntc packfiles→6859/28901/…, lookup 6859→[Main v0], fileId 2440959→base 684223. Build adds `-I ../gw2index ../gw2index/sqlite3.o`.

**STRS RC4 DECRYPT in-browser (2026-07).** New `strs_keys.{h,cpp}` (`gw2skeys`): loads `textkeys.csv` (textId,key8_hex — from live capture) + `strs_textbase.csv` (fileId,baseTextId — from TextPackManifest, all 5 langs, in gw2app/). Ports expand(sub_140D9F630)+RC4(CptRc4)+bitunpack; `decode(data,size,baseTextId)` decrypts packed records (textId=baseTextId+recordIndex → key lookup). main.cpp: File→"Load String Keys…" (ID_FILE_LOAD_KEYS 1006) + `try_autoload_keys()` (exe dir / `..\`) at startup; the `PreviewKind::Strs` case re-decodes via `castlemist::skeys::decode` when `keys_ready()` (baseTextId from `get_by_file_id(dat, mft_index+1)`). Verified: record 659 of strs 2440959 → "Survival Survivor". Keys not captured stay flagged `<no key…>`. See [[gw2-strs-decrypt]].

The single **Preview** tab swaps surface by detected type (`ExtractedEntry::kind` in [entry_extractor.h](castlemist/include/entry_extractor.h)):
- **Image** → `gw2gfx` (d3d_renderer.cpp), letterboxed quad.
- **Model** (`PF` packfile w/ GEOM chunk) → `gw2m3d` (model_renderer.cpp): orbit cam, own HLSL ported from [gw2viewer.cpp](gw2mcp/viewer/gw2viewer.cpp) (NOT DXBC), modes Full/Plain/Wireframe. Geometry+materials via [gw2model.hpp](gw2mcp/native/gw2model.hpp) (needs the struct template).
- **Text/Strs** → read-only edit. STRS via strs_view.cpp (raw-UTF16 exact; packed/`baseChar!=0` records are RC4-locked and only flagged, per [[gw2mcp-server]] notes).

**"Shader" render mode = the GAME's real bgfx DXBC shaders (2026-07).** 4th `RenderMode::GameShader` in [model_renderer.h](castlemist/include/model_renderer.h), toolbar button "Shader" (ID_MODE_SHADER, single-model only; map path excluded per request). Ports the whole [gw2gsviewer.cpp](gw2mcp/viewer/gw2gsviewer.cpp) AMAT/bgfx pipeline into the browser. Split across threads: **bg thread** ([entry_extractor.cpp](castlemist/src/entry_extractor.cpp) `build_model_preview(...,want_game=true)`, set only on the single-model GEOM path so map loads stay fast) traces each material's AMAT (baseId-1 of `materialFile` texture) → `extractAmat` picks VS+PS → `parse_bgfx` → stores raw DXBC + uniform tables + sampler→slot map + renderState in `ModelPreview::gameMaterials` (new `GameMaterial`/`GameSamplerCPU`/`GameShaderUniform` CPU structs in [entry_extractor.h](castlemist/include/entry_extractor.h)); textures decoded via the shared `get_texture` dedup into `ModelPreview::textures`. **UI thread** (`model_renderer.cpp` `build_game_materials`) creates VS/PS via Create*Shader, input layout from D3DReflect ISGN (`game_sem_off`), DYNAMIC cbuffers (`game_build_cb`/`game_update_cb`, bgfx `reg`=byte offset, matrices transposed), SRVs per slot (globals: white 1x1 / grey 1x1x6 env-cube slot 13). `render_game()` = opaque then translucent back-to-front, static daylight-SH + fxclr uniform rig (`setup_game_uniforms`, from gw2gsviewer). VERIFIED headless (GW2_AUTOLOAD + shot_shader.bmp): 291977 renders all 9 submeshes with real normal-mapped jade-tech detail (vs flat-grey Full); 302-bone boss 146006 renders textured with its game shaders (confirmed by pixel-diff vs Full-bind: mean 7.2 — the toggle IS active, it just shades a skin material similarly). **8-UV DONE (2026-07):** browser `GVertex` now carries all 8 UV sets (`u,v`@48 + `uv1[7][2]`@56, **stride 144**, BLENDINDICES@112 BLENDWEIGHT@128 — recon input layout offsets bumped to match); `build_model_preview` copies `castlemist::model::Vertex.uv[7][2]` (F16/F32 already decoded there); `game_sem_off` maps TEXCOORDn→48+8n, BLENDINDICES→112, BLENDWEIGHT→128. This FIXED the 291977 "berantakan texture" (detail/decal layers were all sampling UV0 → garbled; now clean basket-weave/brick). **Lighting toned down (2026-07):** gw2gsviewer's 1.25 "showcase" sun blew a dark boss (Deimos 146006) into a pale self-lit "lamp"; `setup_game_uniforms` now uses a neutral ~0.9 sun (no >1 overdrive), lower SH ambient DC, LightBuffer 0.8, and a grey-190 (not white) global light-buffer stand-in → moody skin matching the concept art. Env **GW2_LIGHT** scales it (default 1.0; raise for bright props). **Game-shader SKINNING DONE via CPU pre-skin (2026-07).** KEY REVERSAL: GW2's material VS take **already-skinned** vertices — dumped uniforms (`amat --out-dir` → .bgfxsh, parsed bgfx v11) show the VS has ONLY ViewProjection/World/WorldView/TexTransform + input sig POSITION/NORMAL/TANGENT/BITANGENT/TEXCOORD (NO BLENDINDICES/BLENDWEIGHT, NO bone-matrix uniform) → the engine skins in a prior pass. So there's nothing to feed bone matrices into; instead `cpu_skin_into(dst)` LBS-skins the rest mesh (`g_cpu_verts`) into a DYNAMIC `g_vb_skinned` each posed frame (same SkinMat = InvWorld·AnimatedWorld as the GPU palette) and render_game binds it when `g_skin_ok && posed`. Verified: Balthazar weapon 556048 head/mask fragments deform under wobble in Shader mode. Also feeds a per-frame **`Time`** uniform (wall clock via QPC) so Time-driven material effects can animate. **STILL open:** (1) animated effect SCROLL (sword 553679 lava, Balthazar `Time`+`perturb/speed/scale` in ps_4) needs the **material-constant defaults** (perturb/speed/scale/glow-color) fed into the cbuffers — we currently zero every uniform not in the static `vals` map, so scroll speed=0 (no motion) and glow tint defaults wrong; plumbing needs matching bgfx uniform-name → MODL `MatConstant.name` (token32 hash, not yet known) or the `AmatShaderBinary.constants[]` defaults (extractAmat doesn't pull them yet). (2) **emissive/additive GLOW billboards blow out to a white blob** on the dark offline bg (Balthazar's white ellipse) — inherent without the real deferred light buffer + effect constants; base rig dimmed (GW2_LIGHT, GW2_GLOBALLIT knobs) fixes normal materials but not self-emissive glow planes. Needs `d3d11shader.h`+`-ld3dcompiler`+`-ldxguid` (already linked). Falls back to reconstruction draw if a material has no usable game shaders (`g_game_any_ok`). See [[gw2-shaders-dxbc]].

**TEXTURE FULL/REDUCED RESOLUTION SELECTOR (2026-07).** GW2 stores many textures as a **full/reduced pair at consecutive MFT indices** (full = baseId B, reduced = B-1, same format, exactly half dims). **A material often references the fileId of the REDUCED member**, so the old loader (`decode_texture_by_fileid` → `mft_data_list[base-1]`) rendered weapons/armor at HALF res (e.g. Balthazar weapon 556048: 256²/512² instead of 512²/1024²; springer 562804, billboard 291982 also affected). Fix in `entry_extractor.cpp`: `resolve_res_index(dat,i0,wantFull)` peeks the neighbor's ATEX header (`peek_mft_atex` = decompress+`castlemist::atex::parse`, dims only, no pixel decode) and detects the pair by same-format + exact 2×/½ dims; the **full member is always the higher index** (i0+1 double ⇒ i0 is reduced; i0-1 half ⇒ i0 is full). Split old decode into `read_mft_atex_bytes`/`peek_mft_atex`/`decode_mft_atex`. Global `std::atomic<bool> g_tex_full_res{true}` + `set_texture_full_res(bool)`/`texture_full_res()` — defined OUTSIDE the file's anonymous namespace (public linkage; the atomic stays inside, reachable TU-wide). UI: **"Full-res tex" push-checkbox** (ID_TEX_FULLRES=2055, checked by default, single-model only) → toggles + re-`on_entry_selected(current_mft_index)` to reload at the chosen size. VERIFIED (GW2_TEXDBG dump): 556048 full=512²/1024² vs reduced=256²/512², chosenIdx=resolvedIdx+1 when the material hit the reduced member; models that reference the full member directly (291977, Deimos 146006) are unaffected (paired=0, no regression). Default is now FULL res.

**LOD + per-submesh texture-size selector (2026-07-15).** GW2 geometry stores LODs as a shared vertex buffer + `ModelMeshGeometryV1.indices` (LOD0) + `lods[]` (array_ptr<ModelMeshIndexDataV1>, each a lower-detail index set over the SAME verts). `gw2model.hpp` `parseGeom` now also reads `lods` → `Mesh.lods` (vector<vector<u32>>); verified model 291977 = LOD0 2080 tris / LOD1 1226 / LOD2 627. Carried through `ModelMeshCPU.lodIndices` → renderer. **model_renderer**: `set_model` packs LOD0+all LODs contiguously into the shared index buffer, recording per-submesh `lodRanges` (vector<pair<start,count>>); the ACTIVE `indexStart/indexCount` are just copied from the selected LOD so every existing DrawIndexed loop follows with no change (`apply_lod`). Texture size = full vs "reduced": each material gets `srvReduced`/`srvNormalReduced` = **CPU half-box-downsample** of the full rgba (`make_srv_half`) — GW2's reduced sibling is ~half-res, so no 2nd dat decode; the Full/reconstruction draw picks reduced SRV per submesh when `s.texReduced` (game-shader mode: LOD works, tex-reduce not wired). APIs (`sub<0` = global/overall, else per-submesh): `submesh_count/label/lod_count`, `max_lod_count`, `set/get submesh_lod`, `set/get submesh_tex_reduced`. **UI** (single model only, `show_model && !show_map`): a SECOND toolbar row (model surface drops one `kToolbarHeight`) with a submesh combo ("All submeshes" + each `mesh k (matN, T tris, L LODs)`), a LOD combo, and a "Reduced tex" push-checkbox (IDs 2062/2063/2064). `populate_lod_controls`/`refresh_lod_controls`/`lod_target_submesh` in main.cpp; called after each `set_model` (main entry + cntc content-sub). Verified headless (`GW2_LODTEST=1 GW2_AUTOLOAD=291976`): shot_lod0 (dense) vs shot_lodmax (sparse, same silhouette) + shot_texreduced (textured, no crash). Also added an image-preview **"Alpha" toggle** (ID 2061, gw2gfx alphaMode uniform) = checkerboard composite vs raw RGB; see [[gw2-atex-atep-decode]].

**MULTI-DIRECTIONAL (HDR-style) reconstruction lighting + intensity slider (2026-07-15).** The Full/Plain reconstruction PS (`kHLSL` in model_renderer.cpp) used ONE directional light (`uLightDir`, `saturate(dot(N,-L))`) -> the side facing away went to ambient only, so a model looked "kadang gelap kadang terang" as you orbit. Replaced with `envLight(N)`: three fixed half-Lambert ("wrap": `d=dot(N,L)*.5+.5; d*d`) directional lights (key .85 / fill .45 / rim .30 from different dirs) + sky/ground hemisphere ambient, all `* uExposure`. Applied to the main material + terrain paths (water/collision keep their special look). New CB field `exposure` (cbuffer `uExposure; float3 _cbpad`, 0 => treated as 1), set from global `g_light_intensity` after every `CB cb{}` reconstruction fill. API `castlemist::render::set_light_intensity/light_intensity` (clamp 0..4). **UI**: a "Light" trackbar (TRACKBAR_CLASSW, ID_LIGHT_SLIDER 2081, range 0..300 -> /100 = 0..3.0) + caption on the model second toolbar row (single model only); `WM_HSCROLL` -> set_light_intensity + invalidate. Needs `ICC_BAR_CLASSES` in InitCommonControlsEx. Env `GW2_LIGHTINT` seeds it in the AUTOLOAD path for headless tests. VERIFIED headless: model mean luminance ~identical across orbit 0/90/180 deg (104.9/105.3/105.7 -- no dark side) and scales with intensity (0.5/1.0/2.5 -> 40/47/60). Distinct from GameShader mode's own sun/SH rig (`setup_game_uniforms`, GW2_LIGHT) and the deferred light pre-pass. See [[gw2-shaders-dxbc]].

**BLENDER-STYLE INTERACTIVE GIZMO + MODERN LIGHT THEME (2026-07-16, user request "gizmo like Blender to know rotation/scale/translation + modernize the whole app").** *Gizmo* (single-model path only; scene/map returns before the draw): new editable **object transform** `g_obj_pos/rot(deg)/scale` in [model_renderer.cpp](castlemist/src/model_renderer.cpp), applied as `model = object_matrix() * vt` where `vt = T(-c)*g_rot*T(c)` is the centering+orbit view transform and `object_matrix = T(-c)*S*R*T(c+pos)` (pivot = model center; identity fast-paths to just `vt`). A per-vertex-**colored line pipeline** (`kGizmoHLSL` VSGiz/PSGiz, `ColVtx{xyz,rgba}`, dynamic `g_gizVb`, LINELIST, drawn with `g_dssNoDepth` so it's always visible) renders: a **ground grid** on XZ under the model (center lines tinted X-red/Z-blue) + the gizmo per **`GizmoMode`** (Translate=colored axes+arrowheads, Rotate=3 axis rings, Scale=axes+box tips) with **X red / Y green / Z blue**, hovered/active axis → yellow. Drawn with `gizMVP = vt*view*proj` (NOT the object matrix) so it sits at the object's transformed pivot (center+pos). **Interaction** (all in model-surface pixels): `gizmo_hit(mx,my)` projects handles to screen (`compute_giz_mvp`+`project_pt`) and returns axis 0/1/2, 3=center, or -1 (point-segment dist for axes/rings, radius for center); `gizmo_begin/update/end` drag — Translate along axis = screen-projected-axis dot of mouse delta × (worldLen/pixelLen); center handle = view-plane via `world_screen_right/up` (= transpose of `g_rot`); Scale = ratio of mouse-dist-from-pivot vs grab; Rotate = screen angle sweep about pivot × sign(axis view-z). `set_model` resets the transform. Public API in [model_renderer.h](castlemist/include/model_renderer.h): `set_gizmo_mode/gizmo_mode`, `set_show_grid`, `get/set/reset_object_transform`, `gizmo_pick/begin/update/end/active`, `set_gizmo_hover`. **UI** (main.cpp): row-2 toolbar **Move/Rotate/Scale** (push-radio) + **Grid** toggle + **Reset XF** (IDs 2083-2087, default = Translate + grid on); `ModelWndProc` mouse: L-down tries `gizmo_begin` first (grab handle → edit, else orbit), move drives `gizmo_update` (drag) / `gizmo_pick`→`set_gizmo_hover` (hover highlight); a **transform readout card** `hwnd_gizmo_readout` (Loc/Rot/Scale, `update_gizmo_readout`) is a **child of `hwnd_model`** (which now has `WS_CLIPCHILDREN|WS_CLIPSIBLINGS`) so the DISCARD-blt DXGI present is clipped around it and it composites over the 3D surface (its `WM_CTLCOLORSTATIC` is handled in ModelWndProc, card bg). VERIFIED headless (`GW2_AUTOLOAD=291977 GW2_GIZMOTEST=1`): grid+move/rot/scale gizmos render, hit-test picks an axis, and a +Y screen-drag moved the object to pos=(0,49.5,0) (X/Z untouched → axis-locked). *Theme*: **light modern**. `enable_visual_styles()` writes a tiny Common-Controls-**v6** manifest to `%TEMP%` and `CreateActCtxW`+`ActivateActCtx` before `InitCommonControlsEx` (+`ICC_STANDARD_CLASSES`) → modern themed buttons/combos/lists/trackbar with NO build/link change. **Segoe UI** font (`ensure_ui_fonts`, -15) applied to every control via `EnumChildWindows(apply_font_cb)` at WM_CREATE end (skips the Consolas text/hex/content tables). Palette `kColBg/Panel/Band/Text/Subtle/Accent/Card/Border` + a `theme_brush(COLORREF)` cache; main window class bg = `theme_brush(kColBg)`; `WM_CTLCOLORSTATIC/EDIT/LISTBOX/BTN` in MainWndProc give white editors/lists, light window bg, dark text, subtle status label. VERIFIED via PrintWindow capture: themed flat toolbar (checked toggles show blue), clean Segoe text, light panels, grid+gizmo in the viewport. Build recipe unchanged.

**MAP GameShader mode + gizmo-in-shader fix + map inset preview + resizable cntc + wrapping toolbar (2026-07-16, user request).** (1) **Shader mode now works on the 3D MAP too** (supersedes the "map path excluded" note above). `model_renderer.cpp`: `build_one_game_mat()` factored out of `build_game_materials` (shared per-material DXBC→VS/PS/IL/CB/SRV builder); `SceneModelGPU` gained `gameMats/gameOk` + model-space `center/radius`; new `build_scene_game_materials(models)` (index-aligned with set_scene props), `scene_game_ready()`, `render_scene_light_prepass()` (scene-wide world-normal G-buffer → light buffer) and `render_scene_game()` (per-instance World/WVP uniforms + each model's game mats into the offscreen HDR target, then `post_process_relight`; non-game models fall back to the reconstruction shader with pre-divided exposure). `render()` scene branch calls it when `g_mode==GameShader && scene_game_ready()`. Extraction is LAZY: `entry_extractor.cpp` `augment_map_scene_game(scene,dat_path)` re-reads each unique model by fileId (new `MapScene::modelFileIds`) and rebuilds it with `want_game=true`; `main.cpp` `ensure_map_game_materials()` runs it (wait cursor) the first time the **Shader** button is picked on a map (`set_model_mode`), so normal map loads stay fast. Shader button now shown on maps. (2) **Gizmo now transforms the MESH in Shader mode** (was skeleton-only): `render_game`/`render_light_prepass` model matrix was missing the editable object transform — both now use `gizmo_identity()?giz_view_transform():mul(object_matrix(),giz_view_transform())` like the reconstruction path (forward-declared the 3 gizmo helpers). (3) **Map inset preview**: click a prop (a click, not a drag) → `pick_scene_instance` (screen-space nearest projected bounding circle) → `set_scene_focus` shows that model in a small bottom-right inset panel (`render_focus_inset`: dark quad via the gizmo color pipeline + the model framed on its own bounds, reconstruction shader, shares scene orbit); right-click or the **Preview** push-toggle (ID_MAP_PREVIEW) clears/hides it. (4) **cntc browser resizable**: two new splitters (ID_SPLIT_CONTENT vertical = browser block↔preview via `content_browser_w`; ID_SPLIT_CONTENT_H horizontal = Types/Entries↔Assets via `content_assets_ratio`). (5) **Wrapping 3D toolbar**: the single-model & map toolbar now flows buttons left-to-right and wraps to new rows when the pane is narrow (grows toolbar height, drops the surface), so nothing is clipped on small screens. VERIFIED headless (`GW2_SCENE=291977 GW2_SCENEGAME=1` → shot_scene_game.bmp shows real bgfx materials distinct from Full's shot_scene.bmp; shot_scene_inset.bmp shows the corner inset). See [[gw2-shaders-dxbc]] [[gw2-map-scene]].

**TWO-PASS TRANSPARENCY + prop-focused map framing (2026-07-16, user: "transparansi tembus pandang tapi depan-belakang jadi satu" & "peta daratan terlihat dikompres").** (1) **Transparency depth ordering:** `g_rsSolid` is `CULL_NONE`, so a translucent submesh (Full/reconstruction effect pass) drew BOTH facings in arbitrary buffer order → near and far surfaces smeared into one flat blob. Added `g_rsCullFront`/`g_rsCullBack` rasterizer states (FILL_SOLID) and the sorted effect/translucent draw now renders each translucent submesh TWICE — cull-front (far/back facets) then cull-back (near/front facets) — so it reads front-over-back. Same total triangles as CULL_NONE (each facet drawn once across the two passes), so flat billboards/additive glow are unaffected; restore `g_rsSolid` after. Applied in all three reconstruction transparent passes: single-model `render()`, map `render_scene()`, and the map inset `render_focus_inset()`. Opaque still CULL_NONE (depth-write handles order). GameShader path left as-is (its own bgfx cull state). Verified on 291977 (jade lattice dome reads with clean near/far layering, no z-fight). (2) **Map framing** — see [[gw2-map-scene]]: `set_scene` now frames the camera on a PROP-only bbox (falls back to prop+terrain only for terrain-only maps) so central props on a huge flat terrain plane no longer render tiny ("compressed"). Verified 181140.

**LIVE CLOTH SIM toggle (2026-07).** A "Cloth" push-button (single-model reconstruction path) runs the reverse-engineered GW2 cloth solver on the current mesh — see [[gw2-cloth-system]]. `castlemist/{include/cloth_sim.h,src/cloth_sim.cpp}` (`castlemist::cloth::ClothSim`) turns the displayed `ModelMeshCPU` into a GrCloth (auto-pins the top ~12% band along Z = GW2 up as the locked prefix, edge constraints from unique LOD0 triangle edges, area-weighted index-buffer normals) and writes deformed pos+normals back into `GVertex`. model_renderer.cpp keeps `g_cloth_rest`+`g_cloth_lod0` per model + a DYNAMIC `g_vb_cloth`; render()'s Full/Plain/Wireframe path steps the sim and streams into it (mirrors the `g_vb_skinned` CPU-skin pattern), so cloth animates while the "Cloth" toggle (ID_CLOTH_TOGGLE 2089, shown only when `has_cloth()`) is on (TIMER_ANIM pumps repaint). API `set_cloth_enabled/cloth_enabled/has_cloth/set_cloth_wind` + `cloth_stats(verts,locked,maxDisp,meanDisp)`. NOT wired into GameShader/map modes. **Headless verify hook `GW2_CLOTHTEST=1` (with GW2_AUTOLOAD=<mftIdx>)**: dumps clothtest.txt + shot_cloth_{rest,draped,wind}.bmp. **VERIFIED on real model 291977: has_cloth=1, cloth_enabled=1, verts=37893 locked=2079, maxDisp 0→100.9 (drape) →212.5 (+X wind); rest vs draped BMPs visibly differ** — the RE'd solver deforms real GW2 geometry in-app. (291977 is a rigid jade dome, not a cape, so it "drapes" as a whole-mesh sag from the pinned top; a real cloth mesh would read as fabric.) The **file-driven** path is now WIRED (2026-07): `castlemist::model::parseCloth` → `ModelPreview::clothPieces` (ClothPieceCPU: own proxy verts/indices/edges/lockCount/params per ModelClothData piece) → `ClothSim::buildFromPieces` (combines all pieces, pinned prefixes gathered to the front, real file constraints) → renderer draws each piece's simulated proxy textured with its material (`render_cloth_proxy`, DYNAMIC `g_vb_clothproxy` + static `g_ib_clothproxy`) and HIDES the static render submeshes whose materialIndex a cloth piece covers (`g_cloth_hide_mat`). `set_cloth_enabled` prefers file pieces (`g_cloth_file_active`), else falls back to the whole-mesh auto-pin demo. **VERIFIED**: fileId 2245424 (mftIdx 614981) — Cloth toggle replaces the static red winged cape with the simulated proxy that visibly droops/ripples under gravity+wind (rest vs draped BMPs differ clearly); stats verts=13864 locked=2466 (= exact sum of the pieces' vertexCounts/lockCounts). fileId 2213170 also works (9 pieces, verts=10083/locked=3571) but is an effect-heavy entity so a poor visual showcase. The old note about the toggle simulating the whole mesh applies only to models with no authored cloth. **CORRECTION (2026-07): the struct template `gw2_packfile.json` DOES fully define the cloth types** — `ModelClothData`/`Constraint`/`MeshGroup`/`BoneWeight` V19–V66, `Obstacle` V29+, `SoftLock` V52+ (238 cloth structs), and **`ModelFileDataV*.clothData` = array_ptr<ModelClothDataV*>** links them, so cloth is reachable via the existing template parser (an earlier note claiming it was absent was a minified-JSON grep artifact). Field layout evolves by version, e.g. `ModelClothConstraintV19{vertIndexA word, vertIndexB word, distance float}` (V33 packs distance as half + a relationship word — see [[gw2-cloth-system]]). Remaining work is to parse `ModelFileData.clothData` in gw2model.hpp and feed `buildFromClothData` so only the authored cloth submesh simulates. NB: **not every model has cloth** — Adelbern (MODL v65, mftIdx 320878 / fileId 24526) has `clothData count=0` (rigid cape); need a model with count>0 to test. **Build** now also needs `-I ../minieditor -I ../minieditor/external/glm-1.0.3` and the sources `../minieditor/GrCloth.cpp ../minieditor/ModelClothData.cpp`. Full build verified clean; solver+bridge headless-validated; interactive GUI check on a real cloth-carrying model still pending.

Struct template (`gw2mcp/templates/gw2_packfile.json`, ~2.2MB) is auto-located at startup (struct_template.cpp `castlemist::tpl::auto_load`) or via **File → Load Struct JSON…**; without it, models show hex + a "load template" note. Model texture decode reloads the dat once per selection on the bg thread (safe, but adds latency).

**Build** (no CMake build dir is used in practice — direct MinGW g++; cmake's "MinGW Makefiles" gen trips on sh.exe in PATH):
```
export PATH="/c/Users/Ridwan Hidayatullah/Documents/codeblocks-25.03mingw-nosetup/MinGW/bin:$PATH"
cd castlemist && g++ -std=c++20 -O2 -DUNICODE -D_UNICODE -mwindows \
  -I include -I ../gw2mcp/native -I ../gw2mcp/native/third_party \
  -I ../dr_libs-master/dr_libs-master -I ../minieditor/external/stb-master -I ../gw2index \
  -I ../minieditor -I ../minieditor/external/glm-1.0.3 \
  src/*.cpp src/stb_vorbis_impl.c ../minieditor/GrCloth.cpp ../minieditor/ModelClothData.cpp ../gw2index/sqlite3.o -o castlemist.exe \
  -ld3d11 -ldxgi -ld3dcompiler -ldxguid -lwindowscodecs -lole32 -lcomctl32 -lcomdlg32 -lgdi32 -luser32 -lshell32 -lwinmm \
  -static -static-libgcc -static-libstdc++
```
(Audio: `-I dr_libs` (MP3) + `-I stb-master` & explicit **`src/stb_vorbis_impl.c`** (Ogg, compiled as C — stb_vorbis.c won't build as C++) + `-lwinmm`. Video: **`-I third_party/bink2` + `-lversion`** — see [[gw2-bink-video]]; no Bink .lib, the DLL is GetProcAddress'd at runtime.) Curated test base-ids in [GW2CURATED.txt](GW2CURATED.txt): MODL 291821/291830/291978, STRS 2925/2930, ATEX 2871; ASND audio idx 2861/2865/3166. **The `-static -static-libgcc -static-libstdc++` at the tail is REQUIRED** — a dynamically-linked build loads the wrong libstdc++/libwinpthread from PATH and dies instantly at startup with `0xC0000139` STATUS_ENTRYPOINT_NOT_FOUND (no window, no WER event; the exe is ~3MB dynamic vs ~5.7MB static). Diagnose "exits in <2s doing nothing" builds with `gdb -batch -ex run <exe>`.

**cntc OBJECTS: real CODENAMES + corrected contentType (2026-07-25, IDA-confirmed).** The client's PackContent **reflection table** (`0x142360e50`–`0x142361050`) gives the authoritative field list: `typeInfos, namespaces, fileRefs, indexEntries, localOffsets, externalOffsets, fileIndices, stringIndices, trackedReferences, strings` + the content byte blob, with sub-structs `PackContentTypeInfo{namespaceIndex,rootIndex}`, `PackContentIndexEntry{...relocOffset...}`, `PackContent{Local,External}OffsetFixup`, `PackContentFileIndexFixup`, `PackContentStringIndexFixup`, `PackContentNamespace{domain,parentIndex}`, `PackContentTrackedReference{sourceOffset,targetOffset}`. Two fixes in `parse_cntc_objects`:
1. **contentType comes from `indexEntry[+0]`, not `content+16`.** PackContentIndexEntry is 16 B = `{u32 contentType, u32 relocOffset, u32, u32}`. Over all 32549 entries of a live cntc, field[0] has **103 clean distinct values** dominated by 35=Item (3323) / 64=Skill (2487) / 0=Achievement, whereas `content+16` has **6573** distinct values — it only coincidentally agrees for some objects, so the old Type column was wrong for many.
2. **Object strings are TWO kinds and must be split by SHAPE, not position.** (First attempt took "first stringIndices fixup in range = the name" and claimed 1:1 — **that was wrong** and produced visibly wrong names for ~2000 objects.) Measured over cntc base 3203: the `strings` table's 21997 entries = **20316 identifier slugs** (`xxxxx.yyyyy`, base64-ish, e.g. `vl8Av.4gynM`) + **1681 readable English labels** that are **SHARED across objects** ("Damage Multiplier" x170, "Integer" x253, "Radius" x120, "Within Range", "AggroRadius", `/Roles[1]/LiveGestureCharrMale`). Per-object fixup counts: **11777 objects have 0 strings, 18764 have exactly 1, ~2008 have 2..144** — so neither "1:1" nor "first = name" holds, and in multi-string objects **the slug is usually LAST**. Fix: `is_content_slug()` classifies by shape → `ContentObject::name` = the slug (identity), `ContentObject::labels` = the readable strings (the real human context). Entries table columns are now **Id | Assets | Slug (id) | Labels**; the asset-less detail card lists the labels. VERIFIED in-app on base 3203: 20957 objects, **20316 with a slug id** (exactly matching the standalone analysis), **2008 with readable labels** — i.e. those 2008 are precisely the ones the old rule mis-named.
Slugs are internal ArenaNet ids, NOT localized display names. Known limitation: a readable label that happens to look like `word.word` (no spaces, 3-8 chars each side) would be misclassified as a slug; only the first slug is taken, the rest fall into labels.
3. **Info panel now reads the pre-computed sizes from the open gw2index DB** (`castlemist::db::lookup(mft_index+1)`, guarded by `is_open()`): indexed type/container, on-disk size, stripped size, **uncompressed size**, verified `actually_compressed` (flags the mis-flagged entries), chunk list, and any index-recorded error. Previously the panel only showed MFT header fields + `entry.decompressed.size()`, so the uncompressed size was unknown for entries whose preview failed to build, even though the DB already had it. Also added the missing `PreviewKind::Audio` / `PreviewKind::Content` labels (they used to fall through to "Binary").
NB slugs are ArenaNet **internal identifiers** (`namespaceish.slug`, obfuscated), NOT localized display names — see the correction in [[gw2-text-pack]]: the cntc→txtm link is unproven and `stringIndices` is emphatically an index into the cntc's own string table, not a textId. Localized cntc names would need per-content-type struct RE in IDA. Also still open: `fileIndices` deref yields 96..86383 while `fileRefs` count is 0, so the "asset fileId" reading (`>40000` filter) may be misinterpreting a **file INDEX** as a raw fileId for some entries — worth auditing.

**OG IMAGE LIBRARIES (libjpeg-turbo / libwebp) + strs TABLE + richer audio (2026-07-25).**
*Images*: new `sdk_image.{h,cpp}` (`gw2img`) decodes stills with the **reference libraries first**, fallback only on refusal: **JPEG → libjpeg-turbo 3.2.0 → WIC → stb_image**; **WebP(RIFF) → libwebp 1.6.0** (no fallback — stb has no WebP, WIC needs an optional codec pack); **PNG → WIC → stb** (libpng 1.6.58 is present in the SDK folder but **needs zlib, which is absent from this MinGW toolchain**, so it's not in the chain yet — drop a zlib in and prepend it). `Image{w,h,rgba,hasAlpha,format,decoder}`; `preview_format_label` now reads e.g. `WebP (lossy)  [libwebp 1.6.0]` so the info panel names the decoder. Replaced the old WIC-only PNG/JPEG/BMP branch in `decompress_raw_entry` with `castlemist::img::is_image`/`decode`. **This gives WebP preview for the first time — the dat's 100 `riff` entries are all RIFF/WEBP/VP8X** (UI atlases, e.g. base 18 = 3968x64), previously unpreviewable. Sources live in `C:\Users\Ridwan Hidayatullah\Documents\sdk-image`; built ONCE into `castlemist/third_party/lib/{libjpegturbo,libwebpdec}.a`:
```
S=".../sdk-image"; B="castlemist/third_party"
# libwebp decode: pure C, no deps. NB include root is src/ (headers at src/webp/).
gcc -O2 -c -I"$S/libwebp-1.6.0" "$S/libwebp-1.6.0/src/"{dec,dsp,utils,demux}/*.c && ar rcs $B/lib/libwebpdec.a *.o
# libjpeg-turbo 3.2.0: hand-written jconfig.h/jconfigint.h/jversion.h in $B/jpegcfg
# (no CMake). MUST compile ALL of src/wrapper/*.c (8+12+16-bit) -- 3.x dispatches
# precision at RUNTIME so the 8-bit core references j12init_*/j16init_*. EXCLUDE
# src/wrapper/template.c (the generator template) and src/jstdhuff.c (include-only).
gcc -O2 -c -I$B/jpegcfg -I"$S/libjpeg-turbo-3.2.0/src" <core .c list> "$S/.../src/wrapper/"*.c && ar rcs $B/lib/libjpegturbo.a *.o
```
Extra build flags: `-I third_party/jpegcfg -I <sdk>/libjpeg-turbo-3.2.0/src -I <sdk>/libwebp-1.6.0/src -I <sdk>/stb-master` + `third_party/lib/libjpegturbo.a third_party/lib/libwebpdec.a`. `sdk_image.cpp` is the one `STB_IMAGE_IMPLEMENTATION` TU (with `STBI_NO_JPEG` since libjpeg-turbo owns JPEG); libjpeg errors are trapped with setjmp/longjmp so a corrupt entry can't exit() the app. VERIFIED in-app (GW2_PARSETEST): base 18/61 → `WebP (lossy) [libwebp 1.6.0]` 3968x64 / 256x24, 515126 → `JPEG [libjpeg-turbo 3.2.0]` 76x69, 25 → `PNG [WIC]` 33x18, all with non-zero pixel sums.
*strs → TABLE*: `PreviewKind::Strs` now renders a 5-column sortable ListView (`hwnd_strs_list`, ID_STRS_LIST 2093: **# | textId | Encoding | Bytes | Text**) instead of one big text blob. New structured APIs: `castlemist::strs::records()` (framing + raw-UTF16 text) and `castlemist::skeys::records(data,size,baseTextId)` (same, plus RC4-decrypts packed records whose key is loaded); rows show the real global textId (baseTextId from the txtm-derived `strs_textbase.csv`) and flag locked records. `lv_add_row` extended to 5 columns.
*Audio detail*: `AppState::audio_sel_info` caches a `castlemist::snd::probe()` of the **selected** clip, so **duration/rate/channels refresh on every clip or bank-sound change, not just on Play** (the old seek bar read `castlemist::snd::duration_seconds()`, i.e. the last *played* clip, showing 0:00 until you hit Play). Info block now lists codec, duration (s + m:ss), sample rate, channels + **Mono/Stereo/5.1** label, bit depth, frame count, ~kbps, encoded size, clip i of N. Dragging the seek bar before pressing Play now loads the selected clip and starts there.

**AUDIO SEEK BAR + new PF summary parsers + index AUTO-LOAD (2026-07-25).** *Audio*: `audio_player.{h,cpp}` gained `duration_seconds()/position_seconds()/seek()` (keeps decoded `g_pcm`+format persistent; `start_at(frameOffset)` re-streams via waveOut from a sample offset; position via `waveOutGetPosition` TIME_SAMPLES + `g_baseFrame`). main.cpp audio toolbar now has a **seek trackbar** (`hwnd_audio_seek`, ID_AUDIO_SEEK 2092, 0..1000 permille) + **"m:ss / m:ss" label** (`hwnd_audio_time`), driven by `TIMER_AUDIO`(2)@10Hz (`update_audio_seek_ui`); WM_HSCROLL scrubs (TB_THUMBTRACK live label, commit-seek on release); reset on entry/clip/combo switch. *Parsers* (entry_extractor.cpp, before the prp2/GEOM checks; `PreviewKind::Text`): `parse_eula_summary` (PackEulaV0: version+per-lang agreement/URL text), `parse_abix_summary` (ABIX/BIDX BankIndexDataV0: per-lang bank fileIds, **PF v1/32-bit**), `parse_textpack_summary` (txtm/txtv/txtV — see [[gw2-text-pack]]), `parse_cscn_summary` (CSCN scene: version + `pf_chunk_listing`). Generic `Pfr` reader + `pf_open(chunk)` derive ptr width from `pfVersion&4`. cntc summary now names the txt* resolution chain. All validated live (GW2_PARSETEST=<baseIds> hook → parsetest.txt): eula 118599 v13/4-lang + 130424 full agreement, ABIX 119671 6 slots, txtm 5133 (1024 strings/file), txtv 69220 (252590 maps), txtV 156894 (8160 variants). *Index auto-load*: `try_autoload_index(hwnd)` opens `gw2_index.db` next to the exe (or ../, cwd) at startup — **called from WinMain right before the message loop, NOT WM_CREATE** (a WM_CREATE exception aborts window creation → same 2s silent exit); wrapped in try/catch; shares `finish_open_index(hwnd,silent)` refactored out of `do_open_index`. Debug hook `GW2_AUDIOTEST` extended to log duration/pos/seek. `.mcp.json` gw2index `GW2_INDEX_DB` repointed to the full `gw2_index.db` (808k-entry Gw2.dat index) from the tiny local_index.db.
